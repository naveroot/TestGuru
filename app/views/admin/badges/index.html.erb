<div class="row justify-content-center">
  <%= render @badges %>
</div>
<div class="row justify-content-center">
  <%= link_to t('links.new_badge'), new_admin_badge_path, class: 'btn btn-primary' %>
  <%= link_to t('links.back'), admin_tests_path, class: 'btn' %>
</div>
