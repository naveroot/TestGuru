<%= form_with(model: [:admin, question, answer], local: true) do |form| %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :title%>
  </p>

  <p>
    <%= form.label :correct %><br>
    <%= form.check_box :correct, id: :answer_correct %>
  </p>

  <p><%= form.submit %></p>
<% end %>