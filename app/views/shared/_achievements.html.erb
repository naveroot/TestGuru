<div class="container-fluid bg-light">
  <div class="d-flex flex-row-reverse">
    <% if current_user.is_a? Admin %>
      <div class="p-2">
        <%= link_to t('.achievements'), admin_badges_path, class: 'btn btn-outline-info' %>
      </div>
    <% end %>
    <%= user_badges(current_user) %>
  </div>
</div>